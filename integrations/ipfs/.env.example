# Copy to .env.local and keep the local file out of Git.

# Required only by processes that upload files.
PINATA_JWT=""

# Public gateway used to download and verify IPFS content.
IPFS_GATEWAY="https://gateway.pinata.cloud"
