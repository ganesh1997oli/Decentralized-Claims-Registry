# Copy this file to .env.local and keep the local file out of Git.

# Required by processes that upload files. A read-only listener needs only the
# gateway setting below.
PINATA_JWT=""

# Public gateway used to download and verify IPFS content.
IPFS_GATEWAY="https://gateway.pinata.cloud/ipfs"
