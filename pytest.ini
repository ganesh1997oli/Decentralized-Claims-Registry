[pytest]
testpaths =
    backend/tests
    integrations
    listener
    model/tests
python_files = test_*.py
markers =
    integration: requires disposable infrastructure configured through TEST_* environment variables
addopts = --strict-markers
