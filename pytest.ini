[pytest]
testpaths =
    apps/backend/tests
    apps/listener/tests
    apps/relayer/tests
    infrastructure/gcp/tests
    packages/integrations
    packages/model/tests
    packages/observability
python_files = test_*.py
markers =
    integration: requires disposable infrastructure configured through TEST_* environment variables
addopts = --strict-markers
